# OpenClaw .env example
#
# Quick start:
# 1) Copy this file to `.env` (for local runs from this repo), OR to `~/.openclaw/.env` (for launchd/systemd daemons).
# 2) Fill only the values you use.
# 3) Keep real secrets out of git.
#
# Env-source precedence for environment variables (highest -> lowest):
# process env, ./.env, ~/.openclaw/.env, then openclaw.json `env` block.
# Existing non-empty process env vars are not overridden by dotenv/config env loading.
# Note: direct config keys (for example `gateway.auth.token` or channel tokens in openclaw.json)
# are resolved separately from env loading and often take precedence over env fallbacks.

# =============================================================================
# Gateway auth + paths
# =============================================================================
# Recommended if the gateway binds beyond loopback.
OPENCLAW_GATEWAY_TOKEN=change-me-to-a-long-random-token
# Example generator: openssl rand -hex 32

# Optional alternative auth mode (use token OR password).
# OPENCLAW_GATEWAY_PASSWORD=change-me-to-a-strong-password

# Optional path overrides (defaults shown for reference).
# OPENCLAW_STATE_DIR=~/.openclaw
# OPENCLAW_CONFIG_PATH=~/.openclaw/openclaw.json
# OPENCLAW_HOME=~

# Optional: import missing keys from your login shell profile.
# OPENCLAW_LOAD_SHELL_ENV=1
# OPENCLAW_SHELL_ENV_TIMEOUT_MS=15000

# =============================================================================
# Security proxy stack
# =============================================================================
# When the security stack is running (security/docker-compose.security.yml),
# all API keys live in security/.env.security — NOT here. The proxies inject
# real credentials at request time so OpenClaw never sees them.
#
# The wiring below tells OpenClaw to route traffic through the proxies.
# These env vars are referenced from openclaw.json via ${VAR} substitution.
#
# See: security/README.md for full setup instructions.
# See: security/.env.security.example for proxy-side credentials.

# -- LLM provider base URL (Anthropic / OpenAI) ------------------------------
# The LLM proxy hides your real API key and optionally scans for prompt
# injection / hidden Unicode. Set this and reference it from openclaw.json:
#
#   "models": {
#     "providers": {
#       "anthropic": {
#         "baseUrl": "${LLM_PROXY_URL}",
#         "apiKey": "unused",
#         "models": [...]
#       }
#     }
#   }
#
# From inside Docker (same compose network):
LLM_PROXY_URL=http://security-llm-proxy:8080
# From the host machine (outside Docker):
# LLM_PROXY_URL=http://localhost:10001

# -- Telegram API base URL ----------------------------------------------------
# Overrides the Telegram Bot API endpoint. Set this and reference it in
# openclaw.json:
#
#   "channels": {
#     "telegram": {
#       "network": {
#         "apiBaseUrl": "${TELEGRAM_API_BASE_URL}"
#       }
#     }
#   }
#
# The api-proxy container intercepts Telegram API calls and injects the real
# bot token. When using the proxy, set botToken in openclaw.json to any
# placeholder (e.g. "proxied") — the proxy replaces it.
#
# From inside Docker (same compose network):
TELEGRAM_API_BASE_URL=http://security-api-proxy:8081
# From the host machine (outside Docker):
# TELEGRAM_API_BASE_URL=http://localhost:10000

# -- Brave Search API base URL ------------------------------------------------
# Overrides the Brave Search API endpoint. Set this and reference it in
# openclaw.json:
#
#   "tools": {
#     "web": {
#       "search": {
#         "brave": {
#           "baseUrl": "${BRAVE_SEARCH_BASE_URL}"
#         }
#       }
#     }
#   }
#
# From inside Docker (same compose network):
BRAVE_SEARCH_BASE_URL=http://security-api-proxy:8082/res/v1/web/search
# From the host machine (outside Docker):
# BRAVE_SEARCH_BASE_URL=http://localhost:10000/res/v1/web/search

# =============================================================================
# Model provider API keys (set at least one)
# =============================================================================
# When using the security proxy stack, do NOT set these here. The LLM proxy
# injects the real key from security/.env.security. Set a dummy value or
# leave empty — the proxy strips the Authorization header and replaces it.
#
# Without the proxy stack, set your real keys here:
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GEMINI_API_KEY=...
# OPENROUTER_API_KEY=sk-or-...
# OPENCLAW_LIVE_OPENAI_KEY=sk-...
# OPENCLAW_LIVE_ANTHROPIC_KEY=sk-ant-...
# OPENCLAW_LIVE_GEMINI_KEY=...
# OPENAI_API_KEY_1=...
# ANTHROPIC_API_KEY_1=...
# GEMINI_API_KEY_1=...
# GOOGLE_API_KEY=...
# OPENAI_API_KEYS=sk-1,sk-2
# ANTHROPIC_API_KEYS=sk-ant-1,sk-ant-2
# GEMINI_API_KEYS=key-1,key-2

# Optional additional providers
# ZAI_API_KEY=...
# AI_GATEWAY_API_KEY=...
# MINIMAX_API_KEY=...
# SYNTHETIC_API_KEY=...

# =============================================================================
# Channels (only set what you enable)
# =============================================================================
# When using the api-proxy for Telegram, do NOT set TELEGRAM_BOT_TOKEN here —
# the proxy injects it. For other channels without proxy support, set tokens
# here as usual.
#
# TELEGRAM_BOT_TOKEN=123456:ABCDEF...
# DISCORD_BOT_TOKEN=...
# SLACK_BOT_TOKEN=xoxb-...
# SLACK_APP_TOKEN=xapp-...

# Optional channel env fallbacks
# MATTERMOST_BOT_TOKEN=...
# MATTERMOST_URL=https://chat.example.com
# ZALO_BOT_TOKEN=...
# OPENCLAW_TWITCH_ACCESS_TOKEN=oauth:...

# =============================================================================
# Tools + voice/media (optional)
# =============================================================================
# When using the api-proxy for Brave Search, do NOT set BRAVE_API_KEY here —
# the proxy injects it. For other tools without proxy support, set keys here.
#
# BRAVE_API_KEY=...
# PERPLEXITY_API_KEY=pplx-...
# FIRECRAWL_API_KEY=...

# ELEVENLABS_API_KEY=...
# XI_API_KEY=...  # alias for ElevenLabs
# DEEPGRAM_API_KEY=...

# =============================================================================
# Docker-specific settings
# =============================================================================
# Config change mode: "pr" = changes go through GitHub PRs (default in Docker),
# "direct" = allow direct writes (local development only).
# OPENCLAW_CONFIG_MODE=pr

# Guard settings (applied by the LLM proxy, not OpenClaw itself)
# GUARD_ENABLED=true
# GUARD_STRIP_HIDDEN_UNICODE=true
